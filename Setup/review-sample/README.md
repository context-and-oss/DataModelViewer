# Reviewer sample

This is a reviewed synthetic DMVPlayground metadata snapshot used by the combined reviewer build. It includes the Person, Project, Planet, and Moon sample tables and their column, relationship, and process-dependency metadata. It contains no business records or environment credentials.

`SHA256SUMS.txt` records the exact `Data.ts` bytes used by the reviewer launcher. The launcher reads this cached snapshot; it does not contact Dataverse or run the generator.
